/**
 * Potential values for the Display Units
 */
enum DisplayUnits {
  /**
   * Use miles (distance) and feet (elevation gain)
   */
  Imperial = 'imperial',   

  /**
   * Use kilometers (distance) and meters (elevation gain)
   */
  Metric = 'metric'
}

export default DisplayUnits;
